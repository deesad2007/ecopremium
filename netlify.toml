# Проект сейчас деплоится на Vercel (см. astro.config.mjs, adapter @astrojs/vercel/serverless).
# Этот файл оставлен на случай возврата к Netlify — тогда нужно сменить адаптер
# на @astrojs/netlify в astro.config.mjs.
[build]
  command = "npm run build"
  publish = "dist"
